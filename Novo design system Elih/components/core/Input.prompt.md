Text field for quote/lead forms ("Cotação simplificada"). Rounded corners (not pill — pills are reserved for buttons/badges), accent-colored focus ring.

```jsx
<Input label="Seu nome" placeholder="Como podemos te chamar?" />
<Input label="E-mail" type="email" error="Informe um e-mail válido" />
```
